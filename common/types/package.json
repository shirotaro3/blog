{
  "name": "types",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "bash -c 'tsc --noEmit'"
  },
  "devDependencies": {
    "typescript": "5.0.4",
    "eslint-config-common": "workspace:*"
  }
}
